[project]
name = "llama-stack-responses"
version = "0.1.0"
description = "Standard Llama Stack starter distribution with OpenAI inference and an OCI MCP sidecar"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "llama-stack-client>=0.3.2",
    "openai>=1.107.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
